@model MessengerWebApp.ViewModels.RegisterViewModel

@{
    ViewBag.Title = "Register";
    Layout = "~/Views/Shared/_RegistrationLayout.cshtml";
}

<h2>Register</h2>
<div>
    or @Html.ActionLink("sign in", "Sign In")
    to your existing account
</div>

@using (Html.BeginForm("Register", "Account", FormMethod.Post))
{
    <div class="form-group">
        <div class="row">
            <div class="col-md-6">
                @Html.DisplayNameFor(m => m.FirstName)
                @Html.TextBoxFor(m => m.FirstName, new { @class = "form-control" })
            </div>
            <div class="col-md-6">
                @Html.DisplayNameFor(m => m.LastName)
                @Html.TextBoxFor(m => m.LastName, new { @class = "form-control" })
            </div>
        </div>
    </div>
    <div class="form-group">
        @Html.DisplayNameFor(m => m.Email)
        @Html.TextBoxFor(m => m.Email, new { @class = "form-control" })
    </div>
    <div class="form-group">
        @Html.DisplayNameFor(m => m.Password)
        @Html.PasswordFor(m => m.Password, new { @class = "form-control" })
    </div>
    <div class="form-group">
        @Html.DisplayNameFor(m => m.ConfirmPassword)
        @Html.PasswordFor(m => m.ConfirmPassword, new { @class = "form-control" })
    </div>

    <input type="submit" class="btn btn-primary btn-block btn-margin-top" value="Register" />

    <div>
        @Html.ValidationSummary(string.Empty, new { @class = "text-danger" })
    </div>
}